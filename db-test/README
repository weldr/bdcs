Download the haskell-rpm repo.  You don't have to build it.  Then:

$ cabal sandbox init
$ cabal sandbox add-source ~/src/haskell-rpm/
$ cabal install --dependencies-only
$ cabal build
$ cabal run import -- metadata.db [list of RPMs here]
